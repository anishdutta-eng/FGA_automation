# Reports

Generated inspection outputs live here — exported slide decks, JSON inspection
records, and any downloaded artifacts.

Files in this folder (other than this README) are ignored by git so that large
binary exports don't get committed. If you want to keep a specific report under
version control, add it explicitly with `git add -f <path>`.
